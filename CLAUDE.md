# CLAUDE.md — 자바 학습 정리 규칙

이 저장소는 **자바를 공부하며 직접 작성한 실습 코드**와, 각 챕터를 마크다운으로 정리한
학습 노트를 함께 관리하는 학습용 repo다. 두 갈래의 학습을 한 곳에 모았다.

- **기본편**: 김영한 — 자바 기본편 (`basic/answer`) — 실습 코드 작성 완료, 정리 진행 예정
- **학교 수업**: 자바 프로그래밍 강의 1~12주차 (`college/`) — 실습 코드 작성 완료, 정리 진행 예정

## 학습 정리 작업 트리거

사용자가 아래와 같은 요청을 하면 **이 규칙대로 학습 정리 마크다운을 작성**한다.

- "N장 정리해줘", "OO 챕터 정리해줘", "지금까지 배운 내용 정리해줘"
- "`N. 제목.pdf` 기반으로 정리해줘", "N주차 정리해줘" 등

## 정리 규칙 (반드시 이 형식 유지)

1. **위치 / 파일명** — 강의별로 구분한다.

   | 강의 | 정리 위치 | PDF 강의자료 위치 (git 무시) | 실습 코드 |
   |------|----------|------------------------------|-----------|
   | 기본편 (김영한) | `basic/docs/javaBasic/NN. 제목.md` | `basic/강의자료/` | `basic/answer/src/<패키지>/` |
   | 학교 수업 | `college/<N>week/README.md` | — (별도 자료 없음) | `college/<N>week/{example,practice}/src/` |

   - 기본편 파일명은 PDF와 동일한 제목에 두 자리 번호를 붙여 `NN. 제목.md`
     (예: `01. 클래스와 데이터.md`). PDF는 `1. 클래스와 데이터.pdf`처럼 한 자리지만,
     정렬을 위해 md는 두 자리로 통일한다.
   - 학교 수업은 별도 docs 폴더 없이 **주차 폴더의 `README.md`가 곧 정리 문서**다.
     새로 쓸 때는 기존 주차 README의 구성(주제 → 파일별 요약 표 → 핵심 정리 → ⚠️ 주의)을 따른다.

2. **정리 소스 (우선순위)**
   - ① **내가 직접 작성한 코드와 주석** — 가장 우선. 내 코드 스니펫과 주석을 실제로 인용해서 설명한다.
   - ② 해당 챕터 **PDF 강의자료** 내용 (기본편에 한함).
   - 둘을 결합하되, **"꼭 알아야 하는 핵심"만** 추린다. 슬라이드 전체 복붙 금지.

3. **작성 스타일 (앞 챕터와 통일)**
   - 맨 위 제목 `# NN. 제목 — 부제`, 그 아래 인용구로 근거(PDF/내 코드 경로) 명시.
   - 섹션마다 `##` 헤딩, 표·코드블록·다이어그램 적극 활용.
   - `⚠️` 주의사항, `💡` 팁, `✅ 핵심 요약` 섹션으로 강조.
   - 서술은 한국어.
   - 학교 수업 정리는 같은 주제의 기본편 정리가 있으면 상호 링크한다
     (예: `05주차. 상속.md` ↔ `09. 상속.md`).

4. **커밋 워크플로우**
   - **`main`에서 바로 작업한다.** 이 저장소는 개인 학습용이라 브랜치나 PR을 만들지 않는다.
   - 커밋 메시지는 **한 줄**, `feat: ...` / `docs: ...` / `chore: ...` 형식 (한국어 요약 허용).
     트레일러(Co-Authored-By 등) 금지.
   - 실습 코드와 학습 정리 md는 **커밋을 분리**한다.

## 진행 계획 (챕터 목록)

### 기본편 — 김영한 자바 기본편

| # | 제목 | 실습 코드 패키지 | 정리 상태 |
|---|------|-----------------|-----------|
| 01 | 클래스와 데이터 | `class1` | ⬜ |
| 02 | 기본형과 참조형 | `ref` | ⬜ |
| 03 | 객체 지향 프로그래밍 | `oop1` | ⬜ |
| 04 | 생성자 | `construct` | ⬜ |
| 05 | 패키지 | `pack`, `com/helloshop` | ⬜ |
| 06 | 접근 제어자 | `access` | ⬜ |
| 07 | 자바 메모리 구조와 static | `memory`, `static1`, `static2` | ⬜ |
| 08 | final | `final1` | ⬜ |
| 09 | 상속 | `extends1` | ⬜ |
| 10 | 다형성1 | `poly` | ⬜ |
| 11 | 다형성2 | `poly` | ⬜ |
| 12 | 다형성과 설계 | `poly/ex` | ⬜ |
| 13 | 다음으로 | — | ⬜ |

**부록** — 챕터 순번과 무관한 배경 지식 정리는 `basic/docs/javaBasic/부록X. 제목.md`로 저장한다.

| 문서 | 내용 |
|------|------|
| 부록A. `new` 인스턴스 생성 과정 — JVM에서 CPU까지 | 바이트코드 → 클래스 로딩 → 힙 할당 → JIT → OS → CPU |

### 학교 수업 — 자바 프로그래밍 (1~12주차)

주차별 정리는 각 주차 `README.md`에 **완료**되어 있다 (실습 코드를 읽고 작성).
인덱스는 [college/README.md](college/README.md).

| 주차 | 주제 | 정리 |
|------|------|------|
| 01 | 자바 시작하기 | ✅ |
| 02 | 자바 기본 프로그래밍 (타입·연산자·조건문) | ✅ |
| 03 | 반복문·배열과 예외 처리 | ✅ |
| 04 | 클래스와 객체 | ✅ |
| 05 | 상속 | ✅ |
| 06 | 자바 기본 API와 `Object` 메소드 | ✅ |
| 07 | 제네릭과 컬렉션 | ✅ |
| 08 | 입출력 스트림과 파일 입출력 | ✅ |
| 09 | 자바 GUI — 스윙 기초 | ✅ |
| 10 | 자바 이벤트 처리 | ✅ |
| 11 | 스윙 컴포넌트 활용 | ✅ |
| 12 | 그래픽 | ✅ |

> 새 챕터 정리를 완료하면 이 표의 상태를 `✅ 파일명` 으로 갱신하고,
> README.md의 진행 상황 표도 함께 갱신한다.

## 프로젝트 메모

### 기본편 — `basic/answer`, `basic/study`
- `basic/answer` — 강의를 따라 친 **정답지**. 빌드 도구 없이 `src/` 하나로 구성된
  IntelliJ 학습 프로젝트. 패키지 = 챕터 단위.
- `basic/study` — 정답지를 보지 않고 다시 작성해 보는 **연습장**. 현재는 빈 폴더다.
  패키지 선언이 `package class1;`처럼 `src` 기준 상대 경로라, 두 프로젝트 간에
  코드를 옮겨도 `package` 문은 고칠 필요가 없다.
  ⚠️ 두 곳을 동시에 소스 루트로 열면 `class1.Student` 같은 클래스가 중복된다.
- 강의자료 PDF는 `basic/강의자료/`에 있으나 `.gitignore`로 커밋에서 제외한다.

### 학교 수업 — `college/`
- VS Code Java 확장으로 만든 주차별 프로젝트. `<N>week/example`(강의 예제),
  `<N>week/practice`(실습 문제) 구조. 일부 주차는 `example`/`practice` 없이 바로 `src/`.
- 컴파일 산출물 `bin/`과 `.vscode/`는 커밋하지 않는다.
